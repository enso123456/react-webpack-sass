import React from 'react';
import ReactDOM from 'react-dom';
import AppNavigation from './navigation/router';

ReactDOM.render(<AppNavigation />, document.getElementById('app'));

module.hot.accept();